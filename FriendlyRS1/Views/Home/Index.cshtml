@{
    Layout = "_Layout";
}

<style>
    .auth-container {
        max-width: 400px;
        margin: 80px auto;
        padding: 40px;
        background: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        text-align: center;
    }

    .auth-title {
        font-size: 2rem;
        font-weight: 600;
        margin-bottom: 20px;
    }

    .auth-icon {
        margin-bottom: 30px; 
    }

    .auth-buttons .btn {
        width: 100%;
        padding: 12px;
        font-size: 1rem;
        border-radius: 8px;
    }

    .auth-buttons a + a {
        margin-top: 15px;
    }
</style>

<div class="auth-container">
    <h1 class="auth-title">Bridge</h1>
    <img class="h-50 w-50 auth-icon" src="/Images/brand.png" alt="Alternate Text" />

    <div class="auth-buttons d-grid">
        <a class="btn btn-primary" asp-controller="Account" asp-action="Login">Log In</a>
        <a class="btn btn-secondary" asp-controller="Account" asp-action="Register">Create Account</a>
        <a class="btn btn-danger" asp-controller="Account" asp-action="ExternalLogin" asp-route-provider="Google">
            <i class="fab fa-google"></i> Sign in with Google
        </a>
    </div>
</div>
